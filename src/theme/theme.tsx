import { createTheme } from "@mui/material";

export const theme = createTheme({
  typography: {
    fontFamily: "DM Sans",
  },
});

export const colorTheme = {
  backgroundColor: {
    primary: "#eff6ff",
  },
  color: {
    primary: "#004ca3",
  },
};
